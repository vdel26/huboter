extends ../layout

block content
  if error
    p= error

  form(method="POST", action="/bots")
    input(type="text", name="name", placeholder="bot name")
    input(type="text", name="adapter", placeholder="adapter")
    input(type="submit", value="create")